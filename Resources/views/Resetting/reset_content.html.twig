{{ form_start(form, { action : path('fos_user_resetting_reset', {'token': token}), method : 'POST', attr : { class : 'fos_user_resetting_reset' } }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'resetting.reset.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
{{ form_end(form) }}
